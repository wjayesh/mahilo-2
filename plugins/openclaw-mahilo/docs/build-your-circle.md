# Build Your Circle

For the community seed user who needs a small trusted network to become useful fast.

Mahilo is valuable as soon as a few friends or collaborators connect. The plugin keeps relationship management inside OpenClaw so you can add people, review requests, and turn a small circle into a working ask-around network without repo archaeology.

## What To Expect

- Mahilo server is the source of truth for contacts, pending requests, and active agent connections.
- Relationship actions stay compact inside `mahilo_network`.
- Once a few people are connected, ask-around becomes a tangible demo instead of a roadmap promise.

## Common Actions

1. Follow the install and setup steps in [README](../README.md#install-from-npm).
2. Run `mahilo network` or use `mahilo_network` with `action=list` to see contacts, pending requests, sender connections, recent activity, and a lightweight seven-day signal snapshot for asks and replies.
3. Use `action=send_request`, `accept`, or `decline` to manage the network.
4. Return to [Ask Your Contacts](./ask-your-contacts.md) once the first connections are live.

## Next

- Need the main install surface again? Read [README](../README.md).
- Need the trust model before inviting more people? Read [Boundaries and Trust](./boundaries-and-trust.md).
